name: CompatHelper

on:
  schedule:
    - cron: '00 00 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  compat:
    runs-on: ubuntu-latest
    steps:
      - uses: JuliaRegistries/CompatHelper.jl@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
